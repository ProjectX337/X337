/**
 * Generated by X337
 *
 * Source:
 * {{ metadata.graph_node if metadata else "unknown" }}
 *
 * Origin:
 * {{ metadata.source if metadata else "unknown" }}
 */

{% for dependency in component.dependencies %}
import { {{ dependency }} } from "lucide-react";
{% endfor %}

interface {{ component.name }}Props {

{% for key, value in component.props.items() %}
  {{ key }}?: {{ value }};
{% endfor %}

  children?: React.ReactNode;

}


export default function {{ component.name }}(
{
{% for key in component.props.keys() %}
  {{ key }},
{% endfor %}

  children,

}: {{ component.name }}Props
) {


  return (

    <div
      className="{{ component.name }}"
    >

      {children}

    </div>

  );

}
